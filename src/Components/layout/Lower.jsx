
const Lower = ({ children }) => (
    <main className="bg-slate-50">
        <div className="max-w-screen-xl mx-auto py-8 sm:px-6 lg:px-8">
            {children}
        </div>
    </main>
);

export default Lower;